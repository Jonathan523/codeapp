language: objective-c
osx_image: xcode14.2 # Choose the appropriate Xcode version

before_install:
  - bundle lock --add-platform x86_64-darwin-21
  - chmod +x downloadFrameworks.sh
  - ./downloadFrameworks.sh

script:
  - xcodebuild -project codeapp/Code.xcodeproj -scheme CodeUI -destination 'platform:iOS Simulator, id:1FBBD280-B85C-4F94-97BF-60B47BFBA2B5, OS:16.2, name:iPad mini (6th generation)' build
